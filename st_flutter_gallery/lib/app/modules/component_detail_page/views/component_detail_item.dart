import 'package:flutter/material.dart';

class ComponentDetailItem extends StatelessWidget {
  const ComponentDetailItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
